# 社内サーバーへの設置手順

このアプリは、社内サーバーに置いて **社内のPCから `http://サーバー名:8080/` で使う** 形にできます。
claude.ai のアカウントもGoogleアカウントも不要で、データは社外に出ません。

サーバーの種類が分からなくても大丈夫なように、**動かし方を2通り用意しています。**
どちらか動くほうを使ってください。

| 方式 | 向いているサーバー | 必要なもの |
|---|---|---|
| **A. Node.js版** | Windows Server、常時起動のPC、Linux | Node.js のインストール |
| **B. PHP版** | NAS（Synology / QNAP）、既にホームページが動いているサーバー | PHP が動くこと（追加インストール不要） |

**どちらを選べばよいか分からない場合は A（Node.js版）を選んでください。**
Node.js版は「自分がWebサーバーになる」ため、社内にWebサーバーがあるかどうかを調べる必要がありません。
PHP版は「既にあるWebサーバーを借りる」方式なので、Webサーバーが動いていることが前提になります。

---

## 最初に — いちばん多い勘違い

**共有フォルダにファイルを置いてダブルクリックしても、データは共有されません。**

ファイル共有とWebサーバーは別のものです。

```
✕ 共有されない
   \\サーバー名\共有\dispatch-board\index.html
   ↓ エクスプローラーからダブルクリックで開く
   → データは各PCのブラウザの中に別々に溜まる

○ 共有される
   ↓ ブラウザのアドレス欄に http://... と入力して開く
   → データはサーバー上の data.json 1か所に集まる
```

見分け方は、**開くときのアドレス**です。

| 開き方 | 共有 |
|---|---|
| `\\サーバー名\...` や `Z:\...`（エクスプローラー） | されない |
| `http://...`（ブラウザのアドレス欄） | される |

### 確かめ方

アプリが自分で判定して、**画面右上に表示します。**

- **「社内サーバーで共有中」** … 共有できています
- **「この端末のみに保存」** … 共有されていません

この表示を**クリックすると**、いまどこにデータが保存されているか、
共有されていない場合は何を直せばよいかが表示されます。迷ったらここを見てください。

---

## ステップ0 — サーバーの種類を確認する

情報システム担当の方がいれば、**「PHPは動きますか？」** と聞くのが一番早いです。
自分で調べる場合は、以下のどれに当てはまるか見てください。

**Windows Server / 常時起動のWindows PC の場合**
→ **A. Node.js版** に進んでください。

**NAS（Synology の DiskStation、QNAP など）の場合**
→ 管理画面に「Web Station」「ウェブサーバー」という項目があれば **B. PHP版**。
　 「Node.js」のパッケージも入れられることが多いので、A も選べます。

**Linuxサーバー（Apache / nginx が動いている）の場合**
→ サーバー上で `php -v` と打って、バージョンが表示されれば **B. PHP版**。
　 表示されなければ **A. Node.js版**。

**社内サーバーが「ファイル共有だけ」の場合**
→ 共有フォルダにファイルを置くだけでは、データの共有保存はできません。
　 **常時電源が入っているPC1台**（事務所の共有PCなど）を選んで、そこで **A. Node.js版** を動かしてください。
　 そのPCの電源が入っている間だけ、他のPCから使えます。

---

## A. Node.js版の手順

### A-1. Node.js を入れる

https://nodejs.org/ja から **LTS版** をダウンロードしてインストールします。
設定はすべて既定のままで構いません。

インストールできたか確認するには、コマンドプロンプト（Windows）またはターミナルで:

```
node -v
```

`v20.x.x` のようにバージョンが出れば成功です。

### A-2. ファイルを置く

サーバー上の好きな場所にフォルダを作り（例: `C:\dispatch-board`）、次のファイルを入れます。

```
index.html     ← アプリ本体
server.js      ← サーバープログラム
start.bat      ← 起動用（Windows）
start.sh       ← 起動用（Mac / Linux / NAS）
```

### A-3. 起動する

**Windows** — `start.bat` をダブルクリックします。
**Mac / Linux / NAS** — ターミナルでフォルダに移動して `./start.sh`

黒い画面に、次のような表示が出れば起動成功です。

```
  回送・納車依頼ボード を起動しました
  ------------------------------------------------
  このPCから      :  http://localhost:8080/
  社内の他のPCから:  http://192.168.1.50:8080/
  ------------------------------------------------
```

**この「社内の他のPCから」のアドレスが、みんなに配るURLです。**
黒い画面は閉じないでください。閉じるとアプリも止まります。

> スタッフには、このURLを**ブラウザのアドレス欄に入力して**開いてもらってください。
> ファイルを配ったり、共有フォルダのファイルを開いてもらったりすると共有されません。
> ブックマークに入れてもらうと以後が楽です。

ポート8080が既に使われている場合は、別のポートで起動します。

```
（Windows）  set PORT=8081
             node server.js

（Mac/Linux） PORT=8081 node server.js
```

---

## B. PHP版の手順

### B-1. ファイルを置く

Webサーバーの公開フォルダ（NASなら `web` フォルダ、Apacheなら `/var/www/html` など）に、
次のファイルを入れます。

```
index.html     ← アプリ本体
api.php        ← サーバープログラム
.htaccess      ← データ保護用（Apacheの場合のみ）
```

`server.js` は使いません。

### B-2. 書き込み権限をつける

このフォルダに **書き込み権限** が必要です（アプリが `data.json` を作るため）。

- **NAS** — 管理画面のフォルダ設定で、Webサーバーのユーザー（`http` など）に「読み書き」を許可
- **Linux** — `sudo chown www-data:www-data /var/www/html && sudo chmod 775 /var/www/html`

### B-3. 開いて確認する

ブラウザで `http://サーバーのアドレス/index.html` を開きます。
画面の右上が **「社内サーバーで共有中」** になっていれば成功です。

> 「この端末のみに保存」と出る場合は、`api.php` が同じフォルダにないか、
> PHPが動いていません。ステップ0に戻って確認してください。

### B-4. nginx を使っている場合

`.htaccess` は Apache 専用です。nginx の場合は設定ファイルに次を追加して、
データファイルが直接ダウンロードされないようにしてください。

```nginx
location ~ ^/data(\.bak)?\.json { deny all; }
```

---

## 管理者とパスワード

承認・差戻しなどの操作は、**管理者としてログインした人だけ**が行えます。

### 登録内容（初期設定）

| 項目 | 内容 |
|---|---|
| 管理者 | 黒田 ・ 稲垣 ・ 深沢 ・ 岩田 ・ 野口 |
| パスワード | `suzuki01`（管理者の共通パスワード） |
| ログインの保持 | 30日間（その後は再入力） |

### 管理者だけができること

- 依頼の **承認** と **差戻し**
- 依頼の **削除**
- **スタッフ設定**（名前・拠点・勤務曜日・勤務時間）の変更

依頼の作成・編集、完了チェックは誰でもできます。

### ログインのしかた

1. 画面右上の **「自分の情報」** をクリック
2. **名前** に上記5名のいずれかを入力（登録外の名前は弾かれます）
3. **役割** を「管理者（承認する）」に変更
4. 現れた **管理者パスワード** 欄に `suzuki01` を入力して保存

ログインすると承認ボタンが表示されます。ブラウザを閉じても30日間は保持されます。

### 変更のしかた

`api.php` の先頭にある次の部分を書き換えて、ファイルを置き直してください。
（Node.js版を使う場合は `server.js` の同じ部分です。両方置いている場合は両方直してください）

```php
const ADMIN_PASSWORD = 'suzuki01';                              // ← パスワード
const ADMIN_NAMES    = ['黒田', '稲垣', '深沢', '岩田', '野口']; // ← 管理者の名前
const TOKEN_DAYS     = 30;                                      // ← ログインを保持する日数
```

**パスワードを変えると、ログイン中の全員が自動的にログアウト**され、次の操作時に再入力を求められます。
管理者から名前を外した場合も同じです。

### セキュリティについて（お読みください）

- パスワードは `api.php` の中に**そのままの文字で**書かれています。
  このファイルを社外に送ったり、公開フォルダ以外の場所にコピーしたりしないでください。
- 承認・削除・設定変更は**サーバー側で拒否**しています。ブラウザの開発者ツールを使っても、
  パスワードを知らない人は承認できません。
- 一方で、**誰がどの名前で依頼を作るかは自由**です（依頼者名は自己申告）。
  厳密な個人ログインが必要になった場合はご相談ください。
- このアプリは**社内ネットワーク内での利用を前提**にしています。
  インターネットから直接アクセスできる場所には置かないでください。

---

## 動作確認

1. サーバーのあるPCで `http://localhost:8080/` を開く（PHP版はサーバーのアドレス）
2. 右上の表示が **「社内サーバーで共有中」** になっているか確認
3. **別のPC** から、案内されたURLを開く
4. 片方で依頼を1件作り、**もう片方の画面に数秒後に出てくれば成功です**
5. 右上の「自分の情報」から管理者としてログインし、**承認ボタンが出れば認証も成功です**

### 「この端末のみに保存」と出るとき

画面右上のその表示を**クリックしてください。** 原因と対処が表示されます。主な原因は次の2つです。

1. **ファイルを直接開いている**（アドレスが `file://` で始まっている）
   → `start.bat` を起動し、表示された `http://...` のアドレスで開き直してください。
2. **サーバープログラムが止まっている**
   → サーバー役のPCで黒い画面が閉じていないか確認してください。起動したら、
   　 診断画面の **「再接続を試す」** を押せばその場でつながります。

### 別のPCから開けないとき

**ファイアウォールがポートを塞いでいる可能性があります。**

Windows の場合、管理者権限のコマンドプロンプトで次を実行するとポート8080が開きます。

```
netsh advfirewall firewall add rule name="Dispatch Board" dir=in action=allow protocol=TCP localport=8080
```

Linux（firewalld）の場合:

```
sudo firewall-cmd --add-port=8080/tcp --permanent && sudo firewall-cmd --reload
```

### サーバーのIPアドレスを調べる

- **Windows** — コマンドプロンプトで `ipconfig` → 「IPv4 アドレス」の値
- **Mac / Linux** — `ip addr` または `ifconfig`

Node.js版は起動時に自動で表示するので、調べる必要はありません。

---

## 自動で起動するようにする（任意）

毎回 `start.bat` を押すのが面倒な場合の設定です。

**Windows — タスクスケジューラ**
1. 「タスクスケジューラ」を開く →「基本タスクの作成」
2. トリガー: **コンピューターの起動時**
3. 操作: **プログラムの開始** → `start.bat` のフルパスを指定
4. 作成後、タスクのプロパティで「ユーザーがログオンしているかどうかにかかわらず実行する」にチェック

**Linux — systemd**

`/etc/systemd/system/dispatch-board.service` を作成:

```ini
[Unit]
Description=Dispatch Board
After=network.target

[Service]
WorkingDirectory=/opt/dispatch-board
ExecStart=/usr/bin/node /opt/dispatch-board/server.js
Restart=always
Environment=PORT=8080

[Install]
WantedBy=multi-user.target
```

```
sudo systemctl enable --now dispatch-board
```

---

## データのバックアップ

すべてのデータは **`data.json` という1つのファイル** に入っています。
このファイルをコピーしておけば、それだけでバックアップになります。

- `data.json` — 現在のデータ
- `data.bak.json` — 直前の状態（自動で作られます）

**戻したいとき** は、アプリを止めて `data.bak.json` を `data.json` に上書きコピーし、再起動します。

社内のバックアップ対象フォルダにこのフォルダごと入れておくのが確実です。

---

## よくある質問

**Q. インターネットに繋がっていないサーバーでも動きますか？**
動きます。見出しの書体だけWeb上のフォントを使っているため、インターネットがない環境では
標準のゴシック体で表示されますが、機能はすべて同じです。

**Q. 同時に何人まで使えますか？**
今回の規模（スタッフ4名、1日数件）であれば余裕があります。
数十人が同時に使うような使い方は想定していません。

**Q. スマートフォンからも使えますか？**
同じ社内Wi-Fiに繋がっていれば、同じURLで使えます。
画面は狭い幅にも対応していますが、週表示は横スクロールになります。

**Q. 誰でも承認できてしまいませんか？**
できません。管理者パスワードを入力した人だけが承認できます。
チェックはサーバー側で行っているため、ブラウザを操作してボタンを出しても承認は通りません。
ただし依頼者名は自己申告です（誰の名前でも依頼を作れます）。

**Q. パスワードを忘れた／変えたい**
`api.php` の先頭に書いてあります。書き換えて置き直せば変更でき、
変更した時点でログイン中の全員が再入力を求められます。

**Q. 古い依頼が溜まってきたら？**
管理者でログインすると、依頼の詳細画面から1件ずつ削除できます。
まとめて消したい場合は、アプリを止めて `data.json` を直接編集するか、
`data.json` を削除すると全データが初期化されます（削除前に必ずコピーを取ってください）。

**Q. アプリの見た目や項目を変えたいときは？**
`index.html` を編集します。作業種別を増やす、勤務時間の初期値を変えるなどは、
ファイルの上のほうにある `JOB_TYPES` と `DEFAULT_SETTINGS` の部分で調整できます。
